# 이 저장소에서 일할 때

## 프로젝트 한 줄

한 엔진(Cloudflare Workers + D1 + R2, 외부 의존성 0개) 위에서 **세 개의 제품**이 갈립니다.
조직마다 `associations.kind` 하나로 화면이 바뀝니다 — 정의는 `cloudflare/src/kinds.js` 한 곳에 있습니다.

| kind | 제품 | 제품 소개 |
| --- | --- | --- |
| `merchant` | 상인회 홈페이지 (멀티테넌트) | `/` |
| `esign` | 전자계약 | `/esign` |
| `franchise` | 모집 랜딩 + 상담 DB | `/homepage` |

코드는 `cloudflare/` 아래가 전부입니다. 저장소 루트의 초기 Node.js 레퍼런스 구현은 제거되었습니다.

## Notion 업데이트 규약 (구현이 끝날 때마다)

**기능 구현·수정을 완료하고 커밋한 뒤에는, 같은 턴에서 Notion 도 갱신합니다.**
Notion MCP(`mcp__Notion__*`)가 연결돼 있지 않으면 건너뛰고 그 사실을 사용자에게 알립니다.

### 독자는 비개발자입니다 — 이게 가장 중요한 규칙

이 워크스페이스의 제품 문서는 **입사한 기획자 신입이 서비스를 이해하려고 읽는 온보딩 자료**입니다.
개발자가 읽는 문서가 아닙니다. 그래서 Notion 에 쓸 때는 저장소에 쓰는 것과 어조를 바꿉니다.

| 쓰지 말 것 | 대신 쓸 것 |
| --- | --- |
| 파일 경로 (`src/kinds.js`), 함수·표 이름 | 그 코드가 하는 일을 사람 말로 |
| `kind=franchise`, `associations.preset` | "조직 유형", "업종 프리셋" |
| Ed25519, SSRF, WCAG AA, LCP | "서명을 잠근다", "남의 서버를 찌르지 못하게", "글자가 읽히는지", "첫 화면이 뜨는 시간" |
| 기능 나열 | **누가 · 왜 · 무엇이 좋아지나** |

반드시 담아야 할 것: 어떤 문제를 푸는가 · 누가 사는가 · 돈은 어떻게 받나 · 화면 흐름 ·
**정직한 한계**(안 되는 것을 적어야 영업이 사고를 안 칩니다).
용어를 처음 쓸 때는 그 자리에서 한 번 풀어 씁니다.

온보딩 허브: `https://app.notion.com/p/3bb0adee652b81aebaf3e0ff607c7d3d`
(제품 구성이 바뀌면 여기 `세 제품 비교`·`용어 사전`·`지금 어디까지 왔나` 절도 함께 고칩니다.)

### 1. 마일스톤 한 줄 추가 — `개발 및 제작 관리` DB

`data_source_id`: `2e30adee-652b-810e-8f87-000b403bf909`

| 속성 | 넣을 값 |
| --- | --- |
| `이름` | 커밋 제목과 같은 어조로 — 무엇이 좋아졌는지가 보이게 |
| `상태` | `완료` (진행 중이면 `진행 중`, 착수 전이면 `시작 전`) |
| `기한` | `date:기한:start` = 완료일 (`YYYY-MM-DD`) |
| `제품` | 아래 제품 페이지 URL 배열 — 여러 제품에 걸치면 여럿 |
| `담당자` | `["8bf51a1f-e0da-4589-bcf9-cf5fb747bd9e"]` |

본문에는 **무엇을 왜 고쳤는지**를 적습니다. 커밋 메시지를 그대로 옮기지 말고,
사용자·수익에 무엇이 달라지는지가 읽히게 씁니다. 실측한 숫자가 있으면 반드시 넣습니다.

한 번의 작업이 커밋 여러 개로 쪼개졌더라도 **의미 단위로 한 줄**만 만듭니다 — 커밋 수만큼 줄이 늘면
타임라인이 못 쓰게 됩니다.

### 2. 제품 페이지 본문 갱신 — `제품 목록` DB

기능이 늘거나 사양이 바뀌면 해당 제품 페이지의 `## 무엇이 되는가 (기능)` 절도 함께 고칩니다.
안 되던 게 되면 `## 알아둬야 할 한계` 에서 그 줄을 지웁니다 — 낡은 한계를 남겨두면
영업이 팔 수 있는 걸 못 팝니다.
(`notion-update-page` 의 `update_content` 로 해당 부분만 교체 — 전체 덮어쓰기 금지)

| 제품 | 페이지 URL |
| --- | --- |
| 상인회 홈페이지 (멀티테넌트) | `https://app.notion.com/p/3bb0adee652b81bc8e1fedeeaa4c9de0` |
| 전자계약 (e-Sign) | `https://app.notion.com/p/3bb0adee652b814483a5e5cd069745f3` |
| 모집 랜딩 + 상담 DB | `https://app.notion.com/p/3bb0adee652b81abb2b5d0e389a9d729` |

`data_source_id`: `2e30adee-652b-8145-a525-000bec085d5a`

두 DB 모두 **유어팀 메인페이지**(`2e30adee-652b-81bd-9ba4-df065fe6f39e`)와
**제품 & 서비스** 페이지에 인라인으로 걸려 있어, 여기만 채우면 두 화면에 함께 뜹니다.

### 하지 말 것

- 새 페이지·새 DB 를 임의로 만들지 않습니다. 위 두 DB 안에서 해결합니다.
- `상태`(제품)를 마음대로 `판매 중` 으로 올리지 않습니다 — 판매 여부는 사용자가 정합니다.
- 확인하지 않은 성능·매출 숫자를 적지 않습니다.

## 테스트

```bash
node --experimental-sqlite --test cloudflare/test/*.test.js   # 로직 (wrangler 불필요)
node --experimental-sqlite cloudflare/scripts/measure.mjs     # 성능 실측
node --experimental-sqlite cloudflare/scripts/a11y.mjs        # 접근성 실측 (WCAG AA)
```
